<!doctype html>
<html>
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <title>{{ title }} — Instant MVP</title>
  <style>body{font:16px system-ui;margin:0;padding:2rem;max-width:720px} .cta{padding:1rem 1.2rem;border:1px solid #111;display:inline-block;text-decoration:none}</style>
</head>
<body>
  <h1>{{ title }}</h1>
  <p>Early access. Limited founding-member pricing.</p>
  {% if checkout_url %}
    <p><a class="cta" href="{{ checkout_url }}">Buy Now</a></p>
  {% else %}
    <p><em>Checkout link not set. Add STRIPE_CHECKOUT_LINK env var.</em></p>
  {% endif %}
</body>
</html>
